{{- if .Values.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "keywatcher.fullname" . }}
  labels:
    {{- include "keywatcher.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "keywatcher.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
{{- end }}
