apiVersion: v1
kind: Service
metadata:
  name: {{ include "keywatcher.fullname" . }}
  labels:
    {{- include "keywatcher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "keywatcher.selectorLabels" . | nindent 4 }}
